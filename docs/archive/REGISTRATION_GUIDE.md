# 🔐 Регистрация и Аутентификация - Полная интеграция с Supabase

## ✅ Что исправлено

Регистрация теперь **полностью интегрирована с Supabase** вместо моковых данных!

---

## 🎯 Обновленный процесс регистрации

### Шаг 1: Создание аккаунта ✅
**Что происходит:**
- Пользователь вводит email и пароль (мин. 6 символов)
- Вызывается `supabase.auth.signUp()`
- Supabase отправляет код подтверждения на email
- Создается запись в `auth.users`

**Код:**
```javascript
const { data, error } = await supabase.auth.signUp({
    email,
    password,
    options: {
        data: {
            telegram: '',
        }
    }
});
```

---

### Шаг 2: Подтверждение email ✅
**Что происходит:**
- Пользователь вводит код из email
- Вызывается `supabase.auth.verifyOtp()`
- Email подтверждается
- Пользователь получает активную сессию

**Код:**
```javascript
const { data, error } = await supabase.auth.verifyOtp({
    email,
    token: emailCode,
    type: 'signup'
});
```

---

### Шаг 3: Добавление Telegram ✅
**Что происходит:**
- Пользователь вводит Telegram username
- Обновляется `user_metadata` через `supabase.auth.updateUser()`
- Telegram сохраняется в профиле

**Код:**
```javascript
const { error } = await supabase.auth.updateUser({
    data: { telegram }
});
```

---

### Шаг 4: Установка PIN ✅
**Что происходит:**
- Пользователь создает 4-значный PIN
- PIN сохраняется в `localStorage`
- Создается запись в таблице `profiles`
- Автоматический вход в систему

**Код:**
```javascript
// Сохранить PIN
localStorage.setItem('bazzar_staff_pin', newPin);

// Создать профиль
const { error } = await supabase
    .from('profiles')
    .upsert({
        id: user.id,
        email: user.email,
        telegram: telegram || null,
        created_at: new Date().toISOString()
    });

// Автоматический вход
onLogin();
```

---

## 🗄️ Структура базы данных

### Таблица `profiles`

```sql
CREATE TABLE public.profiles (
    id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    email TEXT,
    telegram TEXT,
    full_name TEXT,
    avatar_url TEXT,
    role TEXT DEFAULT 'staff',
    balance DECIMAL(10, 2) DEFAULT 0,
    xp INTEGER DEFAULT 0,
    rank INTEGER DEFAULT 1,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Автоматическое создание профиля

При регистрации через Supabase Auth автоматически создается профиль:

```sql
CREATE TRIGGER on_auth_user_created
    AFTER INSERT ON auth.users
    FOR EACH ROW
    EXECUTE FUNCTION public.handle_new_user();
```

---

## 🔒 Row Level Security (RLS)

### Политики безопасности:

1. **Просмотр:** Пользователь видит только свой профиль
```sql
CREATE POLICY "Users can view own profile"
    ON public.profiles
    FOR SELECT
    USING (auth.uid() = id);
```

2. **Обновление:** Пользователь может обновлять только свой профиль
```sql
CREATE POLICY "Users can update own profile"
    ON public.profiles
    FOR UPDATE
    USING (auth.uid() = id);
```

3. **Создание:** Пользователь может создать только свой профиль
```sql
CREATE POLICY "Users can insert own profile"
    ON public.profiles
    FOR INSERT
    WITH CHECK (auth.uid() = id);
```

---

## 🎨 UX улучшения

### Haptic Feedback ✅
- Вибрация при вводе PIN
- Успех/ошибка при регистрации
- Тактильная обратная связь на каждом шаге

**Код:**
```javascript
import { haptic } from '../utils/telegram';

// Успех
haptic.notification('success');

// Ошибка
haptic.notification('error');

// Легкая вибрация
haptic.impact('light');
```

### Toast уведомления ✅
- Информативные сообщения на каждом шаге
- Описание ошибок
- Подтверждение успешных действий

---

## 📊 Процесс входа

### Обычный вход (Email + Password)
```javascript
const { data, error } = await supabase.auth.signInWithPassword({
    email,
    password,
});
```

### Вход через PIN (после первого входа)
- PIN проверяется в `PinLock.jsx`
- Сессия уже существует в Supabase
- Быстрый доступ без повторного ввода пароля

### Биометрический вход (опционально)
- FaceID/TouchID через `useBiometric`
- Еще быстрее чем PIN
- Максимальная безопасность

---

## 🚀 Как применить миграцию

### Вариант 1: Через Supabase Dashboard
1. Открыть Supabase Dashboard
2. Перейти в SQL Editor
3. Скопировать содержимое `supabase/migrations/setup_profiles.sql`
4. Выполнить SQL

### Вариант 2: Через Supabase CLI
```bash
cd /Users/macbookpro/Desktop/BAZZAR\ PRODJECT\'S/BAZZAR\ MARKET/bazzar-staff-mobile

# Применить миграцию
supabase db push

# Или создать новую миграцию
supabase migration new setup_profiles
# Затем скопировать SQL в созданный файл
```

---

## 🧪 Тестирование регистрации

### 1. Запустить приложение
```bash
npm run dev
```

### 2. Открыть в браузере
http://localhost:5173/

### 3. Нажать "Создать аккаунт"

### 4. Пройти все шаги:
- ✅ Ввести email и пароль
- ✅ Проверить email (код придет на почту)
- ✅ Ввести Telegram username
- ✅ Создать PIN (4 цифры)

### 5. Проверить в Supabase Dashboard:
- Таблица `auth.users` - новый пользователь
- Таблица `profiles` - новый профиль

---

## ⚠️ Важные замечания

### Email подтверждение
- Supabase отправляет реальные email
- Проверьте папку "Спам" если не пришло
- Код действителен 24 часа

### PIN код
- Хранится в `localStorage`
- Не отправляется на сервер
- Используется только для быстрого входа

### Telegram
- Опциональное поле
- Можно пропустить (оставить пустым)
- Используется для связи с ботом

---

## 🔧 Конфигурация Supabase

### Email Templates (опционально)
Настройте шаблоны email в Supabase Dashboard:
1. Authentication → Email Templates
2. Выберите "Confirm signup"
3. Кастомизируйте текст и дизайн

### Email Provider
По умолчанию Supabase использует свой SMTP.
Для продакшена настройте свой:
1. Authentication → Settings
2. SMTP Settings
3. Добавьте свой SMTP сервер

---

## 📝 Следующие шаги

### Опциональные улучшения:
1. **Social Login** - вход через Google, GitHub
2. **Password Reset** - восстановление пароля
3. **Email Change** - смена email
4. **2FA** - двухфакторная аутентификация
5. **Session Management** - управление сессиями

---

## ✅ Чеклист

- [x] Supabase Auth интеграция
- [x] Email подтверждение
- [x] Создание профиля
- [x] PIN код
- [x] Haptic Feedback
- [x] Toast уведомления
- [x] RLS политики
- [x] Автоматические триггеры
- [x] Миграция SQL
- [x] Документация

**Регистрация полностью готова к продакшену!** ✅
