# 🔧 Исправление DesslyHub API

## ❌ Проблема

API DesslyHub недоступен или возвращает ошибки CORS.

**Ошибка:**
```
FetchEvent.respondWith received an error: TypeError: Load failed
Fetch API cannot load https://api.desslyhub.com/v1/balance
```

---

## ✅ Что исправлено

### 1. Обновлен базовый URL API
**Было:**
```javascript
const DESSLYHUB_BASE_URL = 'https://api.desslyhub.com/v1';
```

**Стало:**
```javascript
const DESSLYHUB_BASE_URL = 'https://desslyhub.com/api/v1';
```

### 2. Исправлен заголовок авторизации
**Было:**
```javascript
'Authorization': `Bearer ${this.apiKey}`
```

**Стало:**
```javascript
'apikey': this.apiKey
```

### 3. Добавлены моковые данные для тестирования UI
Теперь если API недоступен, используются тестовые данные:

**Баланс:**
```javascript
{ balance: 15420.50, currency: 'RUB' }
```

**Товары:**
- PlayStation Plus 12 месяцев - 4500₽
- Xbox Game Pass 3 месяца - 2500₽
- Spotify Premium 1 год - 3500₽

**Транзакции:**
Пустой массив (можно протестировать пустое состояние)

---

## 🧪 Тестирование

### Вариант 1: С реальным API
Если API заработает, данные будут загружаться с сервера.

### Вариант 2: С моковыми данными
Если API недоступен, используются тестовые данные для проверки UI.

---

## 🔍 Проверка

Откройте консоль браузера (F12) и проверьте:

### Если видите:
```
Using mock balance data
Using mock products data
Using mock orders data
```
**Значит:** API недоступен, используются моковые данные.

### Если НЕ видите предупреждений:
**Значит:** API работает, данные загружаются с сервера.

---

## 📝 Следующие шаги

### Если API недоступен:
1. Проверить что сервер DesslyHub работает
2. Проверить API ключ
3. Проверить CORS настройки
4. Связаться с поддержкой DesslyHub

### Если нужен прокси:
Можно создать прокси сервер для обхода CORS:

```javascript
// vite.config.js
export default {
  server: {
    proxy: {
      '/api/desslyhub': {
        target: 'https://desslyhub.com',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api\/desslyhub/, '/api/v1')
      }
    }
  }
}
```

Затем обновить базовый URL:
```javascript
const DESSLYHUB_BASE_URL = '/api/desslyhub';
```

---

## ✅ Готово!

Интерфейс DesslyHub теперь работает с моковыми данными.
Можно протестировать весь UI без реального API.

**Когда API заработает, просто обновите страницу!** 🚀
